# 碎片 #0001「启程之镇」物品清单

> **物品总数**：61 件 | **P0**: 28 / **P1**: 22 / **P2**: 11
> **设计基准**：`fragment_0001.md` + `物品设计需求规范 V1.0`
> **配套文件**：`0001_manifest.json`、`0001_sp_mask_对应表.md`
> **最后更新**：2026-06-04

---

## 目录

1. [区块一：广场](#区块一广场)
2. [区块二：钟楼及暗室](#区块二钟楼及暗室)
3. [区块三：市集](#区块三市集)
4. [区块四：研究所及解密实验室](#区块四研究所及解密实验室)
5. [区块五：边界光墙区域](#区块五边界光墙区域)
6. [区块六：行政中心](#区块六行政中心)
7. [特效与演出资源](#特效与演出资源)
8. [NPC 关联物品](#npc-关联物品)
9. [UI 组件](#ui-组件)
10. [Shader 穿透物品-Mask 对应表](#shader-穿透物品-mask-对应表)

---

## 区块一：广场

> **设计意图**："完美的假象"——第一印象温暖友善，但细节让玩家逐渐质疑。
> **叙事层级**：Surface

### 环境-地面

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 |
|----|------|------|------|------|------|--------|
| `0001_env_plaza_ground` | 广场石板地砖 | `environment` | 灰白色广场石板铺装模块。主色调 `#C8C4BC`（暖灰白），石板之间的灰缝为 `#A09B94`（冷灰）。每块石板呈不规则矩形（模拟手工裁切），但外轮廓为直线以满足拼接要求。石板表面有轻度磨损——频繁行走的路径区域颜色偏浅（`#D4D0C8`）。**视觉锚点**：石板间的灰缝在 32px 下形成网格——这是可辨识的关键结构。此地面需承接玩家脚印贴花（`#B0ACA5`，半透明，仅玩家脚印可见——NPC走过不留痕）。 | 每模块 `64×64px`（4×4 tiles），无缝平铺。提供 straight/edge/corner 三个变体 | `gray_card` | **P0** |

### 环境-室内/固定装置

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 |
|----|------|------|------|------|------|--------|
| `0001_env_plaza_fountain` | 广场喷泉 | `environment` | 圆形石质喷泉，位于广场正中央。主体为浅灰白色石料 `#C0BCB4`，外圈矮石台（高约 12px），内圈为喷水口。水面 `#D8D4CC` 略微透光——但水面本身没有动态波纹（静态贴图）。水柱为三条细线从中心喷出，`#E8E4DC`（近白）。**视觉锚点**：圆形外轮廓+中心水柱的纵向线——在 32px 下形成"带竖线的圆"。整体为静止状态——水面和水柱在静态 PNG 中呈现定格形态，运行时通过 Shader 叠加描图纸材质实现轻微波动。 | 宽 80px（5 tiles）× 高 96px（6 tiles），圆形直径约 64px | `gray_card`+`tracing_paper`（水面叠加） | **P0** |

| `0001_env_plaza_bench` | 广场长椅 | `environment` | 木制长椅，位于广场东侧。主体为暖灰调木纹 `#A89880`（牛皮纸色），椅腿四条、靠背三条横木。表面有轻度磨损——坐面中央比两端颜色浅（`#B8A890`）。靠背横木之间有窄间隙（约 2px）。**视觉锚点**：椅背的三条横线在缩小后仍清晰——这是区分长椅与其它方形物的关键。放置在广场东角——面向喷泉方向。 | 宽 48px（3 tiles）× 高 32px（椅背最高处），椅面高 16px | `kraft` | **P1** |

### 道具

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 交互方式 | 可携带 | 多状态 |
|----|------|------|------|------|------|--------|---------|--------|--------|
| `0001_prop_sundial_a` | 日晷A·白石 | `prop` | 观测点A日晷——白色大理石材质 `#E8E0D4`（暖白），底座为方形石台 `#D0C8BC`，上方为半圆形晷面（直径 24px），晷面刻有放射状刻度线 `#B0A898`。晷针为细铜色杆 `#B8860B`，投下 12° 的阴影 `#8A8078`（暖灰阴影）。石面有人为刮擦痕迹——晷面左侧有 3 条不规则浅划痕 `#C0B8A8`（比周围亮约 5-8%），暗示"有人改过角度"。底座正面刻有序列号"TP-2077-03-A"，字体为细线衬线体 `#A09888`。**视觉锚点**：半圆+放射线的日晷剪影——在 32px 下通过"圆形+斜线（晷针）"辨识。 | 底座宽 24px×深 16px，晷面直径 24px，总高 36px（含底座） | `gray_card` | **P0** | Godot: Area2D（交互检测），Sprite2D（视觉）。按E键→角度数据自动记录到日志 | 否（固定在场景中） | 单状态 |
| `0001_prop_newspaper` | 折叠报纸 | `prop` | 放置在广场东侧长椅上的折叠报纸。主体为旧书页材质的米黄色 `#E8E0CC`，四折折叠——可见外页为头版，顶部有模糊的大号标题痕迹（灰色墨迹块 `#908878`——不可读文字），下方为两栏模糊段落痕迹。报纸边缘有轻微磨损——右下角有深色烫痕 `#A09078`（直径约 6px 的圆形痕迹）。日期在头版右上角以细小数码体墨迹暗示："2077年3月14日"（数字极小，约 4px 高，`#706858`——需走近才能辨识）。**视觉锚点**：长方形叠纸+顶部标题墨块——在 32px 下通过"带黑条的米色方块"辨识。 | 折叠后宽 20px×高 14px，展开后宽 40px×高 28px | `book_page` | **P1** | 靠近观察（自动弹出放大视图） | 否（留在长椅上） | 单状态（不可拾取——只可观察） |

---

## 区块二：钟楼及暗室

> **设计意图**：钟楼是"时间的谎言"——培训场景中唯一不是公司建造的建筑，是强行整合进来的"真实碎片"。暗室是"第一个真相"。
> **叙事层级**：Surface（钟楼外部）+ Deep（暗室）+ Engaged（钟楼内部）

### 环境-建筑

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 占地面积 | 入口描述 | 屋顶描述 | 墙面材质使用 |
|----|------|------|------|------|------|--------|---------|---------|---------|------------|
| `0001_env_clocktower` | 钟楼建筑 | `environment` | 碎片0001中最古老的建筑——与周围整洁的新建筑风格不协调。**场所身份锚点转化**："钟楼=时间的容器"→ 建筑立面使用"齿轮"作为结构装饰——圆形齿轮窗嵌入墙面（不是真实功能窗，是纸艺切割的圆形+齿边轮廓）。主体墙面为深灰石质 `#8A8278`（比普通建筑暗——暗示年代更久），墙面有明显风化痕迹——纵向的浅色水渍纹 `#A09890`。屋檐为暗灰 `#706860`，缓坡顶。**视觉锚点**：顶部的时钟圆盘（见 `0001_prop_clocktower_clock`）+ 高耸的纵向比例——在 32px 下通过"高矩形+顶部圆形"辨识。**必须保留的可读性**：入口在底部正面——宽 2 tiles 的拱形门洞（`#5A5248` 暗色内部），上方有拱心石（`#9088` 略亮——纸艺折痕表现立体感）。窗在钟面下方左右各一个——暗示内部有楼层。 | 宽 5 tiles（80px）× 高 12 tiles（192px） | `gray_card`（主体）+ `kraft`（门框木饰） | **P0** | 
| 门在正面底部中央，拱形门洞宽 2 tiles（32px），门框为深棕色木饰 `#6B5840`。门本身为木门——纹理纵向，有金属合页（`#B8A890` 细线暗示）。门上方刻有石质门楣——暗灰 `#787068`。 | 
| 缓坡顶——右侧比左侧高约 8px，形成不对称轮廓。屋顶为暗灰 `#706860`，覆盖深灰瓦片（`#686058` 重叠横线纹理）。屋脊线有一条细铜色装饰条 `#A08060`（暗示钟楼内部机械结构）。屋顶右端有一个小型避雷针（`#908880` 细竖线+顶端小三角）。 | 
| 主材质 `gray_card` 覆盖全部墙面。齿轮窗的圆形边框使用 `kraft`（木质感——暗示齿轮是木制的）。门框木饰使用 `kraft`。拱心石为中心单块 `#A09890`（比周围墙面亮）。 | Godot: StaticBody2D。碰撞矩形: (0,0)-(80,192)px。拱形门洞入口在底部中央(24-56px, 192px)——碰撞体留空。齿轮窗位于 y≈32-48px 处左右各一（仅视觉，无碰撞）。

### 环境-室内/固定装置

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 |
|----|------|------|------|------|------|--------|
| `0001_env_clocktower_stairs` | 钟楼台阶 | `environment` | 钟楼入口处的 13 级石质台阶。整体从地面延伸至钟楼入口，每级高 4px、深 6px。台阶主体 `#908880`（中灰石），边缘有轻微磨损——各级前沿边角有亮色擦痕 `#A8A098`。**关键细节**：第 7 级台阶的颜色和阴影与其它台阶完全相同——但在 Godot 中，第 7 级不挂载物理碰撞（视觉上完全一致，但踩上去没有声音——通过代码实现而非视觉区分）。**视觉锚点**：阶梯的锯齿形轮廓——在 32px 下通过"楼梯形"辨识。 | 宽 3 tiles（48px）× 纵深 7 tiles（112px），总高 52px | `gray_card` | **P1** | Godot: StaticBody2D。碰撞矩形:(0,0)-(48,112)px，第7级(第25-28px高度段)无碰撞体 |

| `0001_env_clocktower_interior` | 钟楼底层室内 | `environment` | 钟楼底层内部空间——暗门和台阶所在之处。地面为深灰石质 `#7A7268`（比外墙略暗——室内光照不足），石壁 `#807870`。地面中央刻有巨大的日晷图案——圆形 + 放射线，`#686058`（比地面深约 12%），直径约 28px——暗示此处是"时间锚点"。顶部有微弱光线从钟面缝隙和破旧窗户透入——在地面形成约 4-5 个不规则光斑（`#C0B8A8` 淡金暖光——来自外部的永恒晨曦）。**关键特征**：从室内透过破旧窗户往上看的天空角度与外界不同——fragment_0001.md §7 环境叙事节拍#2："天光的角度和外界不同——这是'内外空间不一致'的暗示"。北侧墙角有通往暗门的石板区域（见 `0001_prop_clocktower_secret_door`）。**视觉锚点**：深灰空间+地面日晷图案放射线——在 32px 下通过"暗色方块+中心放射线"辨识。 | 室内宽 5 tiles（80px）× 高 8 tiles（128px） | `gray_card` | **P0** | Godot: 背景层 StaticBody2D（可通行区域）——墙面碰撞矩形需单独定义：左右墙宽 4px、上墙边界在 128px 处。入口在底部中央（拱形门洞位置），碰撞体留空 |

### 道具

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 交互方式 | 可携带 | 多状态 |
|----|------|------|------|------|------|--------|---------|--------|--------|
| `0001_prop_clocktower_clock` | 钟楼时钟 | `prop` | 钟楼正面顶端的大型圆盘时钟。主体为浅米白色钟面 `#E0D8C8`（旧书页质感——暗示这个钟面被翻阅了无数次），圆形直径约 36px。钟面外圈有 12 个刻度标记——短细线 `#706858`，其中 12 点位置的刻度略粗。两根指针：时针指向"6"偏右约 0.5°（`#504838`），分针指向"12"偏右约 0.5°（`#403828`——比分针略细）。两根指针都有极轻微的位置偏移——暗示不是完全停在 06:12，而是"被锁在了接近 06:12 的位置"。钟面边缘有细铜色边框 `#B8860B`（`foil` 微量使用——不超过钟面面积的 5%）。指针固定不动——这是静态 PNG。**视觉锚点**：圆形+十字指针——在 32px 下通过"带十字线的圆"辨识。 | 钟面直径 36px，边框宽 2px，总直径 40px | `book_page`（钟面）+ `foil`（边框，<5%面积） | **P0** | 视觉观察——无直接交互。钟面的位置是世界的视觉重心 | 否（固定在钟楼正面上方） | 单状态（指针永不动——除非 Shader 演出） |

| `0001_prop_doorframe_inscription` | 门框刻字 | `prop` | 钟楼底部入口门框右侧的刻字。位置在门框石面——约在离地 28px 处。刻字为手刻风格——笔画粗细不均 `#706058`（比石面深约 15%），字高 6px。内容为"太阳停在12分的地方。指针却不一样。"——使用模糊的书写痕迹（不可读的具体文字——在游戏中呈现为刻痕纹理，文案由本地化字段控制）。**视觉锚点**：在 32px 下不明显——需走近（<100px 距离开启放大）才能注意到刻痕的存在。 | 刻字区域宽 16px×高 20px | `gray_card`（石面基底） | **P0** | Godot: Area2D（触发检测，碰撞半径约 50px）。靠近时按E键→显示文字（与解密直接相关） | 否 | 单状态 |

| `0001_prop_sundial_b` | 日晷B·青铜 | `prop` | 观测点B日晷——钟楼底部东侧。青铜材质 `#A08060`（暗铜色），表面有氧化斑——晷面右上方有绿色铜锈区域 `#6B8B6B`（不规则块状，覆盖约 20% 面积）。晷面为圆形（直径 22px），晷针投下 24° 的阴影 `#706050`。**关键细节**：铜面有一道手指印——晷面左下侧——手指宽的浅色擦痕 `#B89870`（比氧化铜面亮约 12%），暗示最近有人碰过。底座为暗灰石 `#888078`。底座刻字"TP-2077-03-B"。**视觉锚点**：暗铜色的圆形+绿色氧化斑——在 32px 下通过"带绿色斑块的暗色圆"辨识。 | 底座宽 22px×深 14px，晷面直径 22px，总高 32px | `gray_card`（底座）+ `kraft`（铜面——用牛皮纸着色模拟铜色） | **P0** | 观察（按E键）→ 角度数据自动记录到日志 | 否 | 单状态 |

| `0001_prop_clocktower_secret_door` | 钟楼暗门 | `prop` | 钟楼底层地板上的隐藏入口——由一整块石板构成。外观上是一块与周围地板几乎相同的方形石板 `#8A8278`——但石板边缘有四条极细的暗缝（约 1px，`#706860`）构成方形轮廓。**视觉锚点**：在地板纹理的掩盖下非常隐蔽——只有观察力强的玩家会注意到四条缝构成了一个方形。暗门有多个状态（见右侧）。 | 暗门宽 3 tiles（48px）× 深 2 tiles（32px），石板缝在 48×32px 的矩形外围 | `gray_card` | **P0** | Godot: Trigger Area2D（解密条件检测）。解密触发：日晷指针校准至 66° → 发出信号 → Got信号后播放石板滑开动画。开启后玩家走入→ 进入暗室（场景切换或Y偏移到暗室层） | 否 | **双状态**：① 关闭（石板与地板纹理一致）② 开启（石板滑开，显示向下通道 `#3A3228` 暗洞，伴随齿轮声——由 FX 系统处理） |

| `0001_prop_chamber_stone_platform` | 暗室石台 | `prop` | 暗室正中央的石质平台。主体 `#706860`（深灰石），顶部表面比四壁更光滑——`#807870`（略有反光）。石台顶部刻有五个角度的序列数字："12 - 24 - 36 - 48 - 66"——手刻风格，笔画粗糙 `#585048`（深灰刻痕），字体大小不一（3-5px 高）。序列旁有手写注释（与序列同一笔迹）:"66。不是60。不是72。是66。"——这些字比数字更难辨认（有意模糊化）。石台外形为长方体——宽 32px×深 20px×高 18px。台面正中央有一个圆形凹陷——直径约 12px——用于放置晨曦之印（`0001_prop_dawn_seal`）。**视觉锚点**：长方体+顶面的白色徽章（当晨曦之印放置时）——在 32px 下通过"方块+圆形亮点"辨识。 | 宽 32px×深 20px×高 18px | `gray_card` | **P0** | 玩家靠近时触发"晨曦之印显现"演出。石台本身不可交互——它是放置印记的基座 | 否 | 双状态：① 无印记（空台）② 有印记（晨曦之印浮于凹陷上方——由 `0001_prop_dawn_seal` 物品管理） |

| `0001_prop_dawn_seal` | 晨曦之印 | `prop` | 碎片0001的源印——一枚嵌入石台顶部的铜色日晷徽章。主体为圆形铜质 `#B8860B`（暗金/古铜色），直径约 12px。徽章表面有浮雕纹——中央为日晷剪影（小三角形晷针+放射线刻度），外圈有细齿边（约 1px 齿距）。徽章边缘在暗门裂缝透入的淡金色光照下泛起微弱的金色光晕——`#D4A020`（`foil` 材质，占徽章面积的 10%）。**视觉锚点**：圆形+金色——在 32px 灰暗环境中是唯一的高光点，天然吸引视线。**动态状态**：在暗门开启前——不存在。通过解密触发演出：徽章从石台凹陷中缓缓升起（FX 控制的透明度过渡，从 0→1，持续约 2 秒）。升起后——徽章持续散发淡金色光晕（脉冲式闪烁，周期约 4 秒——每次脉冲暗约 20% 亮度，持续 0.5 秒）。徽章边缘有纸艺折痕纹理——它是纸质的"徽章"，不是真实金属。 | 直径 12px（徽章主体），光晕外延至 20px | `foil`（徽章边缘光，≤10%面积）+ `kraft`（徽章主体铜色） | **P0** | 靠近观察（自动触发）→ 晨曦之印融入玩家星图。不可手动拾取——演出自动处理 | 是（剧情自动获得——进入星图系统） | **三状态**：① 不存在（解密前）② 显现中（2 秒过渡动画，FX 控制）③ 显现完成（持续脉冲光晕，直至玩家靠近触发送入星图） |

| `0001_prop_handwritten_note` | 手写便笺 | `prop` | 暗线A种子——放置在石台一角抽屉中的泛黄便笺纸。主体 `#E8DCC0`（暖旧米黄色），折成方形（约 14px×14px 折叠态）。纸面有蓝黑墨水手写痕迹 `#203040`（墨色——不可读的具体字，呈现为模糊的段落+日期墨块）。展开后可见：多行段落痕迹（每行约 16px 宽），其中第三行末尾有被涂掉的日期——透过背面能辨识"03"和"15"两个数字 `#182838`（深墨迹）。最后一行笔迹开始发抖（墨迹由实线变为虚线）。纸面左下角有轻微水渍——`#D8CCB0`（直径约 6px 的圆形淡痕）。**视觉锚点**：方形折纸+蓝色墨迹块——在 32px 下通过"带蓝条的米色小方块"辨识。 | 折叠态 14px×14px，展开态 28px×18px | `book_page` | **P0** | 走近石台→便笺微微发光（引导拾取）。按E键阅读→弹出放大视图显示纹理（不可读文字+局部日期） | 否（留在暗室——但内容被记录到日志） | 单状态（不可折叠/展开——始终以折叠态显示，阅读时以 UI 层放大视图呈现） |

| `0001_prop_chamber_chalk_drawing` | 暗室粉笔涂鸦 | `prop` | 暗室北侧石壁上的粉笔涂鸦。石壁基底 `#686058`（深灰），涂鸦为极淡的白色粉笔线条 `#C8C0B0`——透明度约 30%（粉笔被擦过多次后的残留）。图案形状：一个人形（极简笔画——圆形头+直线身体+四肢）站在一个半圆形（日晷）前。人像只有轮廓，没有五官。线条粗细不均（约 1-2px），有多处断点——仿佛画的人在颤抖。右下角有一小块被手掌抹过的区域——约 8px×6px 的模糊灰白区域 `#B0A898`。**视觉锚点**：在白墙（暗灰石壁）上极难发现——只在玩家停在暗室北侧并仔细观察时才能看到。 | 涂鸦区域宽约 24px×高约 32px | `gray_card`（石壁基底） | **P1** | 靠近观察（无提示——纯视觉发现） | 否 | 单状态 |

| `0001_prop_chamber_crack` | 暗室地面裂缝 | `prop` | 暗室石台正下方地面的极细裂缝。外观为一条不规则的细线——`#4A4038`（近乎黑色的深灰），宽 1-2px（变化范围），长 18px（最大范围），走线略呈Z形。**关键特征**：从裂缝中透出微弱的彩色光（不是灰度世界的黑白光——是颜色）。光透出的颜色不是单一色——是柔和的暖光谱覆盖（`#FFE0C0`→`#FFD0A0`→`#FFC080` 渐变），非常微弱（约 15% 不透明度）。彩色光为静态贴图——不规则地分布在裂缝中的 3-4 个"亮点"。**视觉锚点**：在暗室整体黑暗的环境中，裂缝中极微弱的暖色光是从上方俯视时唯一的彩色点——天然吸引视线，但需要玩家走到石台正前方才能看到。在碎片0001中无法打开此裂缝。 | 裂缝长 18px（最大范围）、宽 1-2px（变化范围），彩色光不规则分布约 12px 范围内 | `gray_card`（石地面基底）+ `tracing_paper`（裂缝中透光——叠加层，非 Shader 穿透） | **P1** | 视觉观察（无交互——在0001中无法打开） | 否 | 单状态 |

### 环境-室内/固定装置

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | Godot 集成 |
|----|------|------|------|------|------|--------|-----------|
| `0001_env_chamber` | 暗室环境 | `environment` | 钟楼下方暗室——与钟楼上方石壁材质相同的"真实碎片"残留空间。无窗——唯一光源来自上方暗门缝隙透入的淡金色晨曦 `#D4B880`（极细光条，宽 1-2px，位于顶部中央）。四周石壁 `#5A5248`（深暗灰——比钟楼室内更暗），石面粗糙——有凿痕纹理 `#4A4238`（不规则凹痕）。地面为粗石 `#4A4238`——正中央被略微打磨平整（36px×28px 区域，`#504838`——石台放置区）。天花板极低——高 48px——玩家进入后有明显压迫感。**关键特征**：北侧石壁上有一幅极淡的粉笔涂鸦（见 `0001_prop_chamber_chalk_drawing`）。空间中没有灯光——唯一的"光源"是门缝光和晨曦之印（见 `0001_prop_dawn_seal`）自身的光晕。**视觉锚点**：极暗空间+顶部淡金细光条+中央石台圆形亮点——在 32px 下通过"全暗+单条光缝+圆心光点"辨识。 | 室内宽 5 tiles（80px）× 高 3 tiles（48px） | `gray_card` | **P0** | 背景层 StaticBody2D。墙壁碰撞矩形：左(0-4px)、右(76-80px)、上(0-48px含门缝——门缝位置留 24-32px 宽空隙)、下(0-80px)。地面为可通行区域。光缝透光通过 Shader 叠加淡金渐变 |

---

## 区块三：市集

> **设计意图**："消费即不存在"——市集看似正常但一切都不会被消耗。面包永远 7 个，喷泉声每 8 秒重复。
> **叙事层级**：Engaged

### 环境-地面

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 |
|----|------|------|------|------|------|--------|
| `0001_env_market_ground` | 市集碎石路 | `environment` | 市集区域的地面铺装模块。比广场地砖更粗糙——主色调 `#B0A898`（中灰），碎石纹理为不规则的细小多边形拼贴。碎石之间有更暗的灰缝 `#989088`（深暖灰）。路面有轻微的车辙痕迹——纵向的浅色双线 `#C0B8A8`（两条平行的淡灰线，间隔约 8px），暗示曾有小推车频繁经过。**视觉锚点**：碎石拼贴的不规则纹理+双线车辙——在 32px 下通过"不平整纹理"与广场的规则石板区分。 | 每模块 `64×64px`（4×4 tiles），无缝平铺。straight/corner 两个变体 | `gray_card` | **P1** |

### 环境-建筑

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 占地面积 | 入口描述 | 屋顶描述 | 墙面材质使用 |
|----|------|------|------|------|------|--------|---------|---------|---------|------------|
| `0001_env_bakery` | 面包店建筑 | `environment` | 市集面包店——培训场景中最"诱人"的建筑。**场所身份锚点转化**："面包店=食谱/烤炉/麦穗"→ 食谱书页→屋脊结构（屋脊线模拟翻开的书脊折痕）；烤炉砖拱→门楣造型（拱形门楣为砖块排列——暗示烤炉口）。墙面主色 `#C8C0B0`（暖灰白），比广场建筑略亮——暗示这是"消费场所"应有吸引力。门楣上方有一排麦穗装饰纹——细线浮雕 `#A89880`（约 8条平行弧线）。**关键特征**：正面橱窗（见 `0001_prop_bakery_window`）。**视觉锚点**：拱形门楣+麦穗纹+橱窗——在 32px 下通过"拱形+横排条纹"辨识。 | 宽 6 tiles（96px）× 高 8 tiles（128px） | `gray_card`（主体）+ `kraft`（门楣木纹装饰）+ `book_page`（屋脊书页纹） | **P1** | 
| 门在正面右侧——宽 1.5 tiles（24px），木门 `#8B7050`（深棕），门上有小窗（`#D8D0C0` 半透明——暗示内部有光）。门框为浅灰石 `#B8B0A0`。 | 
| 缓坡顶——屋脊线模拟翻开的书脊折痕（`#C0B0A0` 纵向折线）。屋顶覆盖灰瓦 `#988880`。屋脊中央有一个小型烟囱 `#908878`——暗示烤炉的存在。 | 
| 主材质 `gray_card`。门楣装饰使用 `kraft`（木质感砖拱）。屋脊折痕线使用 `book_page`（书页质感的淡色线条）。橱窗区域为独立材质处理。 | Godot: StaticBody2D。碰撞矩形: (0,0)-(96,128)px。入口（木门）在正面右侧(64-88px, 128px)——碰撞体留空。橱窗位于正面左侧(8-40px, 72-96px)——仅视觉，无碰撞。 |

### 道具

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 交互方式 | 可携带 | 多状态 |
|----|------|------|------|------|------|--------|---------|--------|--------|
| `0001_prop_bakery_window` | 面包店橱窗 | `prop` | 面包店正面左侧的橱窗——由玻璃（`#D8D4C8` 半透明，约 40% 透明度）+木框（`#A08870`）构成。橱窗内部可见：一个木质货架（`#B09880`），上面整齐排列着 7 个面包——7 个面包大小和颜色完全一致（`#C8B898` 暖棕色，面包顶部有十字划痕 `#A08870`）。排列方式为 4 个在前排、3 个在后排——每次玩家经过时排列永远不变（暗示世界没有"消耗"概念）。橱窗玻璃上有轻微的灰尘——`#B0A898` 半透明斑点（3-5 个不规则小点）。**视觉锚点**：橱窗内的 7 个面包形成的整齐排列——在 32px 下通过"横条中的规则圆点"辨识。 | 橱窗宽 32px×高 24px，面包每个约 6px×4px | `gray_card`（橱窗框架）+ `kraft`（货架）+ `book_page`（玻璃——用旧书页模拟半透明） | **P1** | 靠近观察（自动触发——无按键）——只是看到内部 | 否 | 单状态 |

| `0001_prop_sundial_c` | 日晷C·黑石 | `prop` | 观测点C日晷——市集面包店橱窗前。黑石材质 `#585048`（深暗灰——近黑），晷面为圆形（直径 22px），刻有银色刻度线 `#A09890`（在黑色基底上对比度高，是五个日晷中刻度最清晰的一个）。晷针为银色细杆 `#B0A8A0`，投下 36° 的阴影 `#302820`。底座刻字"TP-2077-03-C"。**关键细节**：底座背面（需要绕到日晷背后才能看到）有极小的刻字——见 `0001_prop_sundial_c_inscription`。**视觉锚点**：深黑圆形+银色刻度——在 32px 下通过"近黑的圆"与其它日晷区分。 | 底座宽 22px×深 14px，晷面直径 22px，总高 32px | `gray_card`（黑石底色） | **P0** | Godot: Area2D（交互检测）。按E键→角度数据记录。绕到背后可发现刻字（无提示——纯视觉发现） | 否 | 单状态 |

| `0001_prop_sundial_c_inscription` | 日晷底座刻字 | `prop` | 观测点C日晷底座背面（绕到背后才能看到）的暗线B植入点。刻字由类似钥匙划痕组成——笔画粗糙不规则 `#404040`（比黑石略浅约 10%），字高约 3px。第一行："TP-2077-03-C"（序列号——比前面日晷的序列号更不规整）。第二行由划痕组成:"第3批测试 C号道具 物理交互——通过"。第三行："心理压力测试——"（"——"之后被刮掉了——呈现为约 8px 宽的乱划痕区域 `#484848`）。**视觉锚点**：非常隐蔽——需要玩家绕到日晷背后且近距离观察才可见。 | 刻字区域宽约 14px×高约 10px | `gray_card`（黑石基底） | **P2** | 靠近观察（无提示——纯视觉发现，暗线B种子） | 否 | 单状态 |

---

## 区块四：研究所及解密实验室

> **设计意图**："知识的禁区"——研究所看起来是支持溯光者的地方，但内部有大量关闭的显示屏和复制粘贴的室内。
> **叙事层级**：Engaged

### 环境-建筑

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 占地面积 | 入口描述 | 屋顶描述 | 墙面材质使用 |
|----|------|------|------|------|------|--------|---------|---------|---------|------------|
| `0001_env_research_institute` | 研究所建筑 | `environment` | 白色现代风格建筑——与小镇其它建筑的古典风格不协调。**场所身份锚点转化**："研究所=知识的存储和访问控制"→ 建筑立面为"档案柜"概念——纵向的墙面划分线模拟档案柜的抽屉分格。墙面主色 `#D8D4CC`（近白——比其它建筑更亮更干净），垂直方向有等距的细凹线 `#C0BCB4`（间隔约 12px——模拟档案柜抽屉的边界）。**关键特征**：外窗——透过窗户看到的室内是复制粘贴的（两张完全相同的桌子并排出现——暗示公司偷懒没认真做室内）。**视觉锚点**：白色+纵向等距分隔线——在 32px 下通过"打格子的白方块"辨识。 | 宽 8 tiles（128px）× 高 6 tiles（96px） | `gray_card` | **P0** | 
| 入口在正面中央——宽 3 tiles（48px）的自动滑门（`#D0CCC4` 浅灰+细银边 `#C8C4BC`）。门上有一个小型摄像头（见 `0001_prop_lab_camera_door`）。门上方有"研究所"字样的模糊文字痕迹（`#B0ACA4`——不可读文字块）。 | 
| 平顶——白色平面 `#D0CCC4`，无坡度。屋顶边缘有细银灰色金属包边 `#C0B8B0`。屋顶上方有一个"通风管道"（`#B8B0A8` 矩形块）——暗示内部有设备需要散热。 | 
| 主材质 `gray_card` 全部。纵向分隔线为同材质加深色。金属包边使用 `kraft`（银色染色的牛皮纸）。 | Godot: StaticBody2D。碰撞矩形: (0,0)-(128,96)px。自动滑门入口在正面中央(40-88px, 96px)——碰撞体留空。 |

### 环境-室内/固定装置

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 |
|----|------|------|------|------|------|--------|
| `0001_env_lab_room` | 解密实验室内部 | `environment` | 白色房间内部——研究所的核心功能空间。房间背景为纯白墙面 `#E0DCD4`（近白——比外部更亮）。墙上挂着 6 个显示屏——但只有 3 个是亮着的（`#C8E0F0` 淡蓝白光——"亮着"的屏幕），其它 3 个是关闭的（`#989490` 暗灰——暗示被刻意关闭）。地板为浅灰白 `#D4D0C8`——比室外广场地面更光滑。房间中央放置操作台（见 `0001_prop_lab_console`）。房间光照偏冷白——来自天花板的矩形灯 `#F0ECE0`（模拟荧光灯——但纸艺世界中它是纸质灯罩）。**视觉锚点**：白色空间+3 亮 3 暗的显示屏阵列——在 32px 下通过"亮暗交替的矩形墙"辨识。 | 室内整体宽 6 tiles（96px）× 高 5 tiles（80px） | `gray_card`（墙面地面）+ `tracing_paper`（亮屏透光——叠加层） | **P1** |

### 道具

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 交互方式 | 可携带 | 多状态 |
|----|------|------|------|------|------|--------|---------|--------|--------|
| `0001_prop_lab_displays` | 实验室显示屏 | `prop` | 研究所室内墙上的 6 块显示屏——3 亮 3 暗。每块屏幕为矩形 `#D0CCC4`（边框），内部显示区约 16px×12px。亮的屏幕（3 块）：显示柔和的蓝白光 `#C8E0F0`——屏幕上"有内容"但不可读（模糊的界面布局痕迹——`#A0C8E0` 矩形块模拟窗口，`#90B8D0` 横向线模拟文字行）。暗的屏幕（3 块）：显示暗灰色 `#989490`——屏幕反光处理（有微弱的斜向反光条 `#B0ACA4` 暗示"屏幕是关着的，不是坏的"）。**视觉锚点**：6 个矩形规则的亮暗交替。 | 每块屏幕 18px×14px（含边框），6 块排列为 2 行 × 3 列 | `gray_card`（边框）+ `tracing_paper`（亮屏内透光——叠加层） | **P1** | 陈技术对话场景的背景——不可交互。当操作台警报响起时，所有亮屏同步闪烁红光（FX 控制） | 否 | 双状态：① 正常（3 亮 3 暗）② 警报（3 亮屏变红色闪烁——FX 控制） |

| `0001_prop_lab_console` | 解密实验室操作台 | `prop` | 实验室中央的金属/木质操作台。主体为浅灰白桌面 `#D8D0C4`，桌腿为暗色 `#807868`（四腿）。桌面中央有一块大型触控板（`#B0B0C0` 略蓝灰的矩形——宽约 24px×高约 8px），触控板表面有指纹痕迹——`#A8A8B8` 淡色细纹。陈技术的手指（NPC 动画控制）在触控板上滑动——触控板本身不会亮（屏幕上什么也没打开）。操作台右侧有一叠文件——`#E8E0CC` 旧书页色纸张，约 3-4 张叠放。**视觉锚点**：桌面+中央矩形触控板——在 32px 下通过"方块上的矩形"辨识。 | 宽 40px×深 20px×高 16px（桌面高 16px，总高 28px 含桌腿），触控板 24px×8px | `kraft`（桌面木纹）+ `gray_card`（桌腿）+ `book_page`（文件夹） | **P0** | 陈技术对话场景背景。触控板在陈技术"非脚本时刻"不亮——而在脚本运行时偶尔有微弱的蓝光指示（`#B0D0F0` 极淡光条） | 否 | 双状态（触控板）：① 脚本模式：淡蓝指示光条可见 ② 非脚本模式：触控板完全暗——无光 |

| `0001_prop_sundial_d` | 日晷D·灰岗岩 | `prop` | 观测点D日晷——研究所门口左侧。灰岗岩材质 `#908880`（中灰带细微颗粒纹理——岗岩特征），晷面为圆形（直径 22px），晷面有轻微风化——表面有细小麻点 `#989088`。晷针为暗铁色 `#706860`，投下 48° 的阴影 `#686058`。底座刻字"TP-2077-03-D"。**关键特征**：日晷正上方有一个摄像头（见 `0001_prop_lab_camera_sundial`）——任何站在此日晷前观察的人都在被拍摄。**视觉锚点**：灰岗岩的颗粒纹理+上方摄像头——在 32px 下通过"带小点的灰圆+上方悬挂方块"辨识。 | 底座宽 22px×深 14px，晷面直径 22px，总高 32px | `gray_card` | **P0** | Godot: Area2D（交互检测）。按E键→角度数据记录。观察时——上方摄像头指示灯闪烁（暗示被记录） | 否 | 单状态 |

| `0001_prop_lab_camera_sundial` | 研究所摄像头（日晷D） | `prop` | 日晷D正上方的监控摄像头——悬挂在研究所外墙。主体为深灰色圆柱形 `#686058`（直径约 6px），镜头为黑色圆形 `#302820`（直径约 3px——不透光黑色）。机体右侧有一个小型指示灯 `#FF4040`（红色——脉冲闪烁，周期约 3 秒：亮 0.3 秒→暗 2.7 秒）。摄像头通过一个 L 形支架连接墙面——`#807868`（深灰金属支架）。**视觉锚点**：红色闪烁指示灯——在灰白世界中是少数有色彩动态的元素，天然吸引注意。 | 摄像头主体 6px×8px（含支架总高约 14px），指示灯 2px×2px | `gray_card`（机体）+ `foil`（红色指示灯——微量，<3%面积） | **P1** | 视觉观察（无直接交互）。当玩家在日晷D处观察时——指示灯闪烁频率临时加快（0.3 秒→0.15 秒——暗示"在被关注"） | 否 | 单状态 |

| `0001_prop_lab_camera_door` | 研究所摄像头（入口） | `prop` | 研究所入口门上方的监控摄像头。与日晷D上方的摄像头外观相同——但指示灯颜色为蓝色 `#4080D0`（区别于红色——暗示此摄像头属于"常规监控"而非"重点监控"）。位置在门楣上方正中。 | 同 `0001_prop_lab_camera_sundial` | `gray_card`+`foil`（蓝色指示灯） | **P2** | 视觉观察 | 否 | 单状态 |

---

## 区块五：边界光墙区域

> **设计意图**："遮盖的真相"——公司用蓝色光墙遮住培训场景的边界。触碰光墙暴露真相。
> **叙事层级**：Engaged + Deep

### 环境/FX 混合

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 触发条件 | 持续时间 | 叠加方式 | 影响范围 |
|----|------|------|------|------|------|--------|---------|---------|---------|---------|
| `0001_env_light_wall` | 蓝色边界光墙 | `environment` | 小镇北边的物理边界——一道半透明蓝色能量幕墙。主体为垂直的蓝色半透明面 `#6088D0`（不透明度约 40%），幕墙上有极细的纵向能量线——`#80A8E0` 亮线间隔约 12px（模拟能量流动）。幕墙从地面延伸到画面顶部（全高）。**关键特征**：光墙本身有微弱的脉动——颜色明度以约 6 秒周期从 40% → 55% → 40% 缓慢变化（模拟"呼吸"效果——通过 Shader 参数控制，不是美术资产）。光墙后的区域——背景为模糊的灰色 `#A0A098`（均匀灰色——没有细节——暗示"未渲染的贴图"）。光墙两端各有一根控制柱（见 `0001_prop_light_wall_pillar`）。**视觉锚点**：蓝色半透明面+纵向亮线——在 32px 下通过"蓝色条纹面"辨识——这是灰白培训场景中唯一的"色彩"（蓝色）。 | 光墙宽约 20 tiles（320px）× 从地面到画面顶（全高，约 192px） | `tracing_paper`（蓝色幕墙——描图纸叠加）+ `gray_card`（外区域灰色） | **P0** | 静态存在——玩家靠近时触发赵安保对话。触碰交互触发 FX 演出 | 持续存在（场景级固定元素） | Overlay（叠加在背景上） | 场景级 |

### 道具

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 交互方式 | 可携带 | 多状态 |
|----|------|------|------|------|------|--------|---------|--------|--------|
| `0001_prop_light_wall_pillar` | 光墙控制柱 | `prop` | 光墙两端的控制柱——共 2 根，左右各一。柱体为深灰色圆柱形 `#686058`（高约 48px，直径约 8px），顶部有一个小型屏幕——屏幕显示蓝色文字块 `#80B0F0`（不可读的模糊字符+数字）。屏幕内容约 5 行：第 1 行显示"边界稳定性：99.7%"（`#80B0F0` 模糊字块）。屏幕偶尔闪烁——`#A0D0FF` 亮闪持续约 0.1 秒。在闪烁的瞬间——第 5 行出现一行快速刷新后消失的小字："...act E-11 containment breach probability: 0.03%"（`#6090D0` 更暗的模糊字块）。柱体底部有黑色底座 `#484038`（宽 10px×高 4px）。**视觉锚点**：竖直柱体+顶端蓝色亮屏——在 32px 下通过"带蓝点的暗色竖线"辨识。 | 柱体 8px×48px，屏幕 8px×12px，底座 10px×4px | `gray_card`（柱体）+ `tracing_paper`（屏幕透光——叠加层） | **P1** | 靠近观察（自动触发——无按键）——屏幕闪烁内容 | 否 | 单状态 |

| `0001_prop_sundial_e_ruins` | 日晷E·残骸 | `prop` | 观测点E日晷——白大理石材质 `#E8E0D4`（与日晷A同色），但已被砸碎。主体不再是完整的日晷——而是散落在石板地面上的碎片。碎片共有 7-8 块不规则形状——最大的一块是晷面的下半部分（弧形碎片，约 14px 宽），上面残留 2 条刻度线。晷针完全断裂——分为两段，一段在底座残骸旁、一段在约 16px 外。碎片边缘为锋利的不规则断口（`#D0C8BC` 比石面颜色更浅的断口截面）。**关键特征**：碎片被砸碎后——被人整齐排列成一行——从底座残骸位置指向光墙方向（碎片的排列线指向北——指向光墙）。排列非常刻意——不是随机散落。每块碎片之间的间距约 2-3px。底座残骸上仍可辨认序列号"TP-2077-03-E"的前半段（"TP-2077-"完整，"03-E"被砸碎）。**视觉锚点**：与其他四个完整日晷形成强烈对比——"本该有日晷的地方只有碎石"。在 32px 下通过"散落的浅色小块+缺失的完整圆形"辨识。 | 残骸散布范围约 28px×24px，最大碎片 14px×8px | `gray_card` | **P0** | 观察（按E键）→ 触发"日晷已被砸碎。无法采集数据。"提示。赵安保对话触发 | 否 | 单状态（始终为碎裂状态） |

| `0001_prop_boundary_floor_inscription` | 边界光墙刻字 | `prop` | 暗线C种子——刻在边界光墙前石板地面上（靠近观测点E残骸处）的文字。石板地面基底 `#B0A898`，刻字为深灰色 `#585048`（比地面深约 40%——确保可读），笔画深浅不一——像是在不同时间、由同一个人分多次刻完的。内容："上一个你走到这里的时候，光墙是红色的。"（模糊的书写痕迹——在游戏中通过不可读的刻痕纹理呈现，文案由本地化字段控制）。刻字排列为横向一行——长约 48px，字高约 5px。**关键特征**：笔画深浅错落——暗示刻字者多次返回此地加深刻痕。**视觉锚点**：在正常视距下不明显——只有走到日晷E残骸附近才会注意到地面的刻痕。 | 刻字区域宽约 48px×高约 7px | `gray_card`（石板基底） | **P1** | 靠近观察（按E键）→ 显示文字。暗线C种子——此时玩家无法理解 | 否 | 单状态 |

| `0001_prop_patrol_vehicle` | 赵安保巡逻车 | `prop` | 边界光墙旁的自动巡逻车——赵安保的"伙伴"。车辆为小型四轮电动车——主体 `#686058`（深灰金属感），车厢为开放式——一个座位（驾驶位），车后部有一个小型屏幕（约 8px×6px——显示"边界接触事件日志"的文字块 `#80B0F0`）。车轮为四个黑色圆形 `#383028`（直径约 8px，有简单轮辐纹理 `#484038`）。车头有一个小型蓝色警灯 `#6090F0`（不闪烁——常亮）。车辆停放在光墙控制柱左侧约 3 tiles 处——始终静止。**视觉锚点**：四个圆形车轮+车顶警灯——在 32px 下通过"带蓝点的四轮车"辨识。 | 车体宽 28px×高 16px（含车轮），屏幕 8px×6px | `gray_card`（车体）+ `kraft`（车轮橡胶质感）+ `foil`（蓝色警灯，<5%面积） | **P1** | 视觉观察（无直接交互）——巡逻车屏幕在玩家触碰光墙后显示事件日志 | 否 | 双状态（屏幕）：① 无事件："无异常事件记录"② 有事件：显示事件日志（但赵安保可能不记录——由剧情控制，不改变视觉） |

---

## 区块六：行政中心

> **设计意图**："官方叙事的源头"——王主管在这里宣讲公司版本的世界观。空间本身暗示"不真实"——回声太快，电子屏播放的是循环片段。
> **叙事层级**：Surface

### 环境-建筑

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 占地面积 | 入口描述 | 屋顶描述 | 墙面材质使用 |
|----|------|------|------|------|------|--------|---------|---------|---------|------------|
| `0001_env_admin_center` | 行政中心建筑 | `environment` | 镇中最"官方"的建筑——天枢公司行政中心。**场所身份锚点转化**："行政中心=公告/宣讲/权威"→ 建筑立面模仿"公告栏"——正面外墙被划分为规则的矩形格子（模拟张贴公告的栏板），格子内有模糊的文字块痕迹。墙面主色 `#D0CCC4`（官方灰白——比其它建筑更"干净"更"正式"），正面上方悬挂天枢公司金色logo（见 `0001_prop_admin_logo`）。门前有三级台阶（`#C0B8B0`）。建筑风格为对称式——左右完全对称（强化"官方""严肃"的视觉感受）。**视觉锚点**：金色logo+格子状立面——在 32px 下通过"带金色方块（logo）的规则灰方块"辨识。 | 宽 8 tiles（128px）× 高 8 tiles（128px） | `gray_card` | **P0** | 
| 入口在正面正中央——宽 3 tiles（48px）的双扇玻璃门（`#D8D4CC` 半透明——暗示内部明亮）。门框为金色金属 `#C0A860`（细框，约 2px 宽）。 | 
| 缓坡顶——对称双坡，屋脊居中。屋顶覆盖深灰瓦 `#888078`。屋脊两端各有一个金色装饰球 `#C0A860`（直径约 4px）。 | 
| 主材质 `gray_card` 全部。logo 使用 `foil`（不超过建筑总面积的 3%）。金色门框和屋脊装饰使用 `foil` 微量。 | Godot: StaticBody2D。碰撞矩形: (0,0)-(128,128)px。入口(双扇玻璃门)在底部中央(40-64px, 128px)——碰撞体在此处留空 |

### 环境-室内/固定装置

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | Godot 集成 |
|----|------|------|------|------|------|--------|-----------|
| `0001_env_admin_hall` | 行政中心大厅 | `environment` | 行政中心内部的大厅——王主管进行世界观宣讲的地点。对称式大厅——左右完全镜像（强化"官方""严肃"的视觉感受）。高天花板 `#C0B8B0`（约 72px），两根深灰石柱位于左右两侧 `#A09890`（宽 8px，纵贯空间），地面为抛光灰石 `#C8C0B0`——光洁度远高于室外广场地面。正面是宣讲台——一个略高的木质讲台 `#B09880`（宽 24px×高 8px），王主管站立于此。讲台上方悬挂天枢公司 logo（同 `0001_prop_admin_logo`）和大型电子屏（见 `0001_prop_admin_screen`）。大厅后方有三排长椅（`#A89880`——与广场长椅同款），暗示"这里是听宣讲的地方"。光照：天花板中央的纸质矩形灯 `#F0ECE0`——模拟荧光灯但只有纸艺的柔和感。声学特征：空间回声极快（约 0.3 秒回音）——暗示墙壁材料不是真实石材，而是轻质隔板。**视觉锚点**：对称大厅+金色logo+电子屏——在 32px 下通过"对称矩形+金色亮点+蓝色屏幕"辨识。 | 室内宽 8 tiles（128px）× 高 6 tiles（96px） | `gray_card`（墙面/柱/地面）+ `kraft`（讲台/长椅木纹） | **P1** | 背景层 StaticBody2D。墙壁碰撞矩形: 左(0-4px)、右(124-128px)、上(0-96px)、下(0-128px——入口位置留空 40-64px)。王主管 NPC 站立位置: (64px, 80px) |

### 道具

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 交互方式 | 可携带 | 多状态 |
|----|------|------|------|------|------|--------|---------|--------|--------|
| `0001_prop_admin_logo` | 天枢公司金色logo | `prop` | 行政中心正面上方悬挂的公司logo。主体为圆形徽章 `#C0A860`（暗金色——箔纸材质），直径约 16px。内部图案：抽象化的星芒图形——从圆心向外的六条放射线（`#D4C080` 亮金线），线之间为暗纹 `#A09050`（深金）。logo 下方有一条金色横幅——`#C0A860` 细长条（宽 20px×高 3px）——上面有不可读的公司名称文字痕迹 `#A09050`。logo 表面有箔纸特有的不规则反光——左上角有一块略亮的区域 `#D8CC90`（模拟左上光源在箔纸上的高光——不超过 logo 面积的 10%）。**视觉锚点**：金色圆形+放射线——在 32px 下通过"金色圆+光芒"辨识——是整个灰白小镇中最亮的点之一。 | logo 圆形直径 16px，含横幅总高约 22px | `foil`（主体——箔纸金色，克制使用——仅 logo 区域使用箔纸） | **P0** | 视觉观察 | 否 | 单状态 |

| `0001_prop_admin_screen` | 行政中心电子屏 | `prop` | 行政中心大厅内的大型电子显示屏——王主管宣讲时的背景。屏幕为矩形 `#D0CCC4`（边框——宽 36px×高 24px），显示区播放万象的介绍画面——循环片段：① 万象的城市全景（`#C8D8E0` 淡蓝灰建筑群轮廓）→ ② 用户在万象中互动（`#D0D8D8` 模糊人形轮廓）→ ③ 天枢logo（`#C0A860`）→ 循环。画面质量很高但都是循环片段——敏锐的玩家会注意到第 47 秒的画面循环断点（极其微弱的闪烁 `#E0E8F0` 持续约 0.5 秒——画面跳到循环开头）。**关键特征**：电子屏画面的第 47 秒（每循环）有一帧闪过一张照片——一个中年男人和一个小女孩在公园。照片为暖色调 `#E0D0B0`（浅棕褐底色——与万象的蓝灰画面形成色温对比），持续时间仅 0.5 秒——然后被正常画面覆盖。**视觉锚点**：大型矩形+动态内容。 | 屏幕 36px×24px（显示区），边框 2px，总 40px×28px | `gray_card`（边框）+ `tracing_paper`（屏幕显示区——透光叠加层，通过 Shader 参数控制循环，0.5 秒照片帧为独立纹理） | **P1** | 玩家进入行政中心时屏幕自动播放循环画面。照片帧超高概率被错过——这是给极细心玩家的彩蛋 | 否 | 循环播放（通过 Shader 控制帧切换） |

---

## 特效与演出资源

> **说明**：以下 FX 物品中涉及的动画时序（秒数）和像素偏移，标注了近似值。"约"在此处表示"以引擎实际调参结果为准"——FX 效果需要在实际游戏中反复微调才能达到最佳视觉感受。精确值将在 Godot 调参后冻结。

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 | 触发条件 | 持续时间（调参基准） | 叠加方式 | 影响范围 |
|----|------|------|------|------|------|--------|---------|---------|---------|--------|
| `0001_fx_light_wall_ripple` | 光墙波纹光晕 | `fx` | 玩家触碰蓝色光墙时——触碰点产生的波纹扩散效果。从触碰点（玩家手指位置）向外扩散的椭圆形蓝色光晕——颜色从 `#80B0F0`（亮蓝，中心）→ `#6090D0`（中蓝）→ `#4068A0`（暗蓝，外圈）→ 透明。波纹共有约 3-4 圈——最内圈直径约 8px，最外圈直径约 40px。波纹速度：每圈间隔约 0.15 秒。波纹扩散过程中——光墙被波纹覆盖的区域短暂变为透明（透出外景 FX 见 `0001_fx_light_wall_transparent`）。**视觉锚点**：圆形波纹+蓝色渐变。这是纸工风格的水波效果——使用描图纸叠加实现。 | 最大波纹直径约 40px（以触碰点为中心） | `tracing_paper`（描图纸蓝色渐变叠加） | **P0** | 玩家触碰光墙一次→触发波纹 | 一次性，约 0.6 秒完成全部波纹扩散 | Additive（叠加在光墙表面） | 触碰点周围约 40px 范围 |

| `0001_fx_light_wall_transparent` | 光墙透明外景 | `fx` | 光墙波纹扩散过程中——光墙短暂透明的效果。在波纹覆盖区域内——光墙的蓝色幕墙变为透明（透明度从 40% → 5%），透出外景的"真实世界"：草地（`#90A870` 暖绿——不同于灰调世界的颜色）、树木（`#788860` 暗绿轮廓）、远处的山（`#889098` 蓝灰远山剪影）、天空（`#D0D8E0` 淡蓝白天）。**关键特征**：这是碎片0001中唯一出现"颜色"的区域——绿草和蓝天的色彩饱和度约为 40-50%（不是灰阶世界的 0-5%）。视觉效果仅持续 0.2 秒——波纹扩散过后，光墙恢复蓝色半透明，外景消失，变回模糊灰色 `#A0A098`。**视觉锚点**：彩色与灰色世界的强烈对比——0.2 秒的闪现足以在玩家记忆中留下"那边有东西"的印象。 | 与波纹范围相同——约 40px 直径圆形区域 | `tracing_paper`（透明过渡）+ 外景为独立纹理 `#90A870` / `#788860` / `#D0D8E0` | **P0** | 光墙波纹扩散过程中自动出现——与 `0001_fx_light_wall_ripple` 同步 | 约 0.2 秒——波纹过后即消失 | Overlay（替换光墙不透明度——不是真正的透明，是视觉替换） | 波纹覆盖区域 |

| `0001_fx_secret_door_open` | 暗门开启效果 | `fx` | 钟楼暗门开启时的演出效果。包含三个视觉元素：① 地板石板滑动——石板从暗门位置向两侧滑开（约 0.8 秒动画），露出向下通道 `#3A3228`。② 从暗门中冒出微弱的暖金色光 `#D4B880`（描图纸叠加——从暗室内部向上透出），光照强度从 0 → 40%（渐变约 1.5 秒）。③ 暗门开启瞬间——有极细的灰尘粒子从石板缝隙中飘出（`#C0B8A8` 3-5 个微小纸屑粒子——上升约 12px 后消散，持续约 2 秒）。**视觉锚点**：地板变动+暖光透出——暗示"地下有东西"。 | 石板滑动范围约 48px×32px，向上透光范围约 56px×40px | `tracing_paper`（暖光叠加）+ `gray_card`（石板滑动——使用与地板相同材质） | **P0** | 玩家在钟楼日晷B处将指针校准至 66° → 触发暗门开启 | 一次性——石板滑动约 0.8 秒，暖光渐变约 1.5 秒，粒子约 2 秒 | Replace（石板滑动）+ Additive（暖光叠加） | 暗门所在区域 |

| `0001_fx_sundial_wrong_angle` | 日晷指针橙光反馈 | `fx` | 玩家在钟楼日晷B处旋转指针至"接近但不正确"的角度（60°）时的反馈效果。日晷指针短暂发出橙色微光 `#E8A040`（描图纸叠加），亮度从 0% → 40% → 0%（持续约 0.8 秒，脉冲式——亮两次）。同时指针轻微回弹——视觉上指针向右偏移约 2px 然后弹回原位（通过 Shader 位移参数控制——约 0.4 秒来回）。**视觉锚点**：橙色微光+指针回弹——明确告诉玩家"接近了，但不对"。 | 仅影响日晷指针区域——约 24px 范围内 | `tracing_paper`（橙光叠加） | **P1** | 玩家在日晷B处将指针旋转至 60°→触发 | 约 0.8 秒（两次脉冲） | Additive（叠加在指针上） | 单个物品 |

| `0001_fx_sundial_correct_angle` | 日晷指针金光成功 | `fx` | 玩家在钟楼日晷B处将指针旋转至正确角度（66°）时的成功反馈。日晷指针发出淡金色微光 `#D4C080`（箔纸描图纸叠加），亮度从 0% → 60% → 稳定在 20%（淡金色持续发光——不再消失）。同时指针发出 3 圈极细的金色波纹（`#D4C080`→`#C0B080`→透明，从指针中心向外扩散——类似光墙波纹但更细腻更慢，每圈约 0.3 秒间隔）。成功反馈触发后约 1.5 秒——暗门开启效果（`0001_fx_secret_door_open`）自动触发。**视觉锚点**：金色光+指针不再回弹——与橙光回弹形成明确对比。 | 仅影响日晷指针区域——约 24px 范围内，波纹扩散至约 36px | `tracing_paper`（金光叠加）+ `foil`（仅指针尖端——微量，<5%面积） | **P0** | 玩家在日晷B处将指针旋转至 66°→触发 | 淡金光持续发光（永久——直到玩家离开碎片），波纹约 0.9 秒 | Additive（叠加在指针上） | 单个物品 |

| `0001_fx_dawn_seal_glow` | 晨曦之印淡金光晕 | `fx` | 晨曦之印在石台上升起后的持续光晕效果。徽章周围（约 16px 半径）有一圈柔和的淡金色光晕 `#D4B880`→`#C0A060`→透明。光晕为脉冲式——约 4 秒周期：亮度从 30% → 50% → 30%，每次亮度峰值持续约 0.5 秒。光晕叠加方式为 Additive——让徽章在暗室中成为唯一的光源感。光晕对暗室墙面有微弱影响——北侧石壁靠近石台的区域被略微照亮（`#686058`→`#787068`——通过 Shader 逐像素光照或预烘焙光贴图）。**视觉锚点**：暗室黑暗中的唯一暖光——在"整个画面偏暗"的环境中形成天然视觉焦点。 | 光晕半径约 16px（以徽章中心起算） | `tracing_paper`（光晕叠加——多层描图纸）+ `foil`（徽章边缘反光，<10%面积） | **P0** | 晨曦之印在石台上显现→光晕自动开始 | 持续循环（脉冲式，4 秒周期）——直到玩家靠近触发送入星图为止 | Additive | 暗室内（主要影响约 40px 直径范围） |

| `0001_fx_lin_datapad_glitch` | 林指导数据板乱码 | `fx` | 林指导的数据板屏幕在对话中偶尔出现的乱码效果。正常状态下数据板屏幕为淡蓝白光 `#C8E0F0`（`tracing_paper` 叠加——显示"正常"的不可读数据）。乱码状态下：屏幕上出现快速滚动的字符块——`#60D0F0` 亮蓝和 `#F04040` 红交替闪烁（持续约 1 秒），字符块为不规则矩形（模拟乱码——不可读），然后恢复正常的淡蓝白光。**视觉锚点**：数据板突然的红色闪烁——在蓝白为主的界面中非常显眼。**叙事触发**：在林指导对话节点 1（初次见面——玩家问"归源计划是什么？"）和节点 4（幕间——数据板掉落）时触发。 | 数据板屏幕区域约 16px×12px | `tracing_paper`（乱码叠加层——通过 Shader 参数切换） | **P1** | 特定对话节点触发：① 林指导查看"第几批"数据时（节点 1）② 幕间数据板掉落时（节点 4） | 每次约 1 秒，在对应对话节点中播放 1-2 次 | Replace（替换屏幕显示内容——短暂切换到乱码纹理） | 单个物品（林指导数据板屏幕） |

---

## NPC 关联物品

> 以下物品为 NPC 持有的独立道具——它们需要独立制作（不是角色纸偶的一部分），但它们在叙事上归属某个 NPC。

| ID | 名称 | 关联NPC | 分类 | 描述 | 尺寸 | 材质 | 优先级 | Shader穿透 |
|----|------|---------|------|------|------|------|--------|-----------|
| `0001_prop_linguide_datapad` | 林指导数据板 | 林指导 | `prop` | 林指导手中捧着的发光数据板。主体为矩形平板 `#D0CCC4`（浅灰白外壳——纸艺切割），屏幕为淡蓝白发光区 `#C8E0F0`（`tracing_paper` 叠加透光）。屏幕显示内容为不可读的模糊数据块——浅蓝灰色 `#A0C8E0` 横线模拟文字行，右上角有条形指示器（`#80B0D0` 细条）。数据板背面有天枢公司logo（与 `0001_prop_admin_logo` 同款但缩小——直径约 4px）。**关键特征**：数据板是林指导"脚本模式 vs 非脚本模式"的视觉信号——脚本模式时她始终手持数据板（屏幕亮），非脚本模式时她偶尔将数据板放回口袋（屏幕暗）。数据板掉落在地上（幕间节点 4）时的状态——屏幕朝下，只能看到灰色背面。**视觉锚点**：矩形平板+蓝色发光面——在 32px 下通过"带蓝光的矩形"辨识。 | 平板宽 18px×高 12px，屏幕显示区 14px×9px | `gray_card`（外壳）+ `tracing_paper`（屏幕透光）+ `foil`（背面logo，<3%面积） | **P0** | — |
| `0001_prop_chentechnology_console` | 陈技术触控板 | 陈技术 | `prop` | 解密实验室操作台上的大型触控板（已在前文 `0001_prop_lab_console` 中作为操作台的一部分描述——此处为 NPC 关联的独立道具视角补充）。触控板表面 `#B0B0C0`（蓝灰色），有细微的指纹痕迹 `#A8A8B8`（淡色细纹——陈技术每天校准触控板留下的）。正常运行时不发光——但在"脚本演示模式"下，触控板边缘有极淡的蓝光指示条 `#B0D0F0`（仅边缘 1px 宽）。**关键区别**：陈技术的手指（NPC纸偶动画控制）在触控板表面滑动时——滑动轨迹会短暂留下浅色拖痕 `#C0C0D0`（约 0.5 秒后消失——通过 Shader 控制）。 | 触控板 24px×8px（与 `0001_prop_lab_console` 一致） | `gray_card`+`tracing_paper`（蓝光边缘） | **P0** | — |
| `0001_prop_zhaosecurity_device` | 赵安保腰间装置 | 赵安保 | `prop` | 赵安保腰间携带的合规度记录装置。主体为小型黑色矩形 `#403828`（约 8px×5px），外壳为硬质纸艺切割——边缘有棱角。装置顶部有一个小型指示灯——多色切换：正常待机时为蓝色 `#6080D0`（常亮），触碰光墙警告时为红色 `#F04040`（闪烁——周期约 0.5 秒），装置上有一个按钮——赵安保的手在警告时移到按钮上但没有按下。按钮为银色 `#B0A8A0`（圆形，直径约 2px）。**视觉锚点**：黑色小矩形+彩色指示灯——在赵安保纸偶上非常小但是唯一有色彩变化的点。 | 装置 8px×5px，指示灯 1.5px×1.5px，按钮直径 2px | `gray_card`（外壳）+ `foil`（指示灯——极微量，<5%面积） | **P1** | — |
| `0001_prop_npc_badge` | NPC胸牌 | 全部四个NPC | `prop` | 所有四个 NPC 左胸口袋上方佩戴的标准公司 ID 胸牌。胸牌为矩形卡片 `#E8E0D0`（奶油白底），宽约 10px×高约 6px。正面印刷内容分为三区：① 顶部大字行——"天枢 TRAINING" `#505048`（深灰模糊字块，字高约 2.5px），② 中间大号序列号——"TP-2077-03-0[数字]" `#404038`（字高约 2px，数字随NPC变化——林指导=1，陈技术=2，王主管=3，赵安保=4），③ 最下方极小字行——"冥府协议合规审查·已通过" `#908880`（极淡灰色模糊字块，字高约 1.2px——需要走近到约 30px 距离才能辨识）。胸牌四角为圆角（半径约 1px），边缘有细银灰边框 `#B8B0A8`。**视觉锚点**：白色小矩形+深灰横条纹——在 32px 下仅能看到"白色小方块"，但不足以阅读任何文字。这是暗线B的关键植入点——"冥府协议"首次出现。 | 胸牌 10px×6px，"冥府协议"小字区域约 8px×1.5px | `book_page`（胸牌底——旧书页白卡纸质感）+ `gray_card`（银灰边框） | **P0** | — |

---

## UI 组件

> 碎片0001中使用的 UI 组件——部分引用 Core 全局资产，部分为碎片的专属 UI（如合规度指示器）。

| ID | 名称 | 分类 | 描述 | 尺寸 | 材质 | 优先级 |
|----|------|------|------|------|------|--------|
| `0001_ui_compliance_bar` | 合规度指示条 | `ui` | 碎片0001 MONITORED 模式专属 UI——蓝色进度条+文字。位于屏幕左上角（约离边缘 16px），整体 64px×8px。背景条为暗灰 `#585048`（圆角矩形，半径 2px），填充条为蓝色渐变 `#6080D0`→`#80A0E0`（亮度随合规度百分比变化——100% 时最亮，<25% 时变为红色 `#D04040`）。填充条左侧有文字标签——"合规度"三个字（`#C0B8B0` 淡灰模糊字块，字高约 3px），右侧有百分比数字——"100%"（`#C0B8B0`，字高约 2.5px）。**视觉锚点**：蓝色横条+百分比数字——在 32px 下通过"蓝条"辨识。背景使用 `book_page`（旧书页质感——UI 面板的统一材质）。 | 整体 64px×8px（含标签） | `book_page`（底）+ `tracing_paper`（蓝色填充——部分透明叠加） | **P0** |

| `0001_ui_hint_terminal` | 半解密提示终端消息 | `ui` | 半解密提示系统的消息弹窗——陈技术发送的文本提示。使用 Core 全局资产 `ui_dialogue_panel` 的变体——但颜色偏蓝（`#B0C8E0` 淡蓝灰底——暗示"系统消息"）。消息为矩形卡片 `#D8DCD0`（比普通对话框略蓝灰），显示不可读的模糊文字行 `#687888`（约 2-3 行模糊字块）。左下角有发送者标签——"半解密提示系统"（`#8098B0` 模糊字块）。消息弹窗位置在屏幕中央偏下——出现时从屏幕底部滑入（约 0.3 秒过渡），持续显示约 5 秒后自动消失或玩家按确认关闭。**视觉锚点**：蓝色调的矩形消息卡——与普通对话的暖黄调面板区分。 | 消息卡片宽 80px×高 24px | `book_page`（卡片底，染蓝调）+ `tracing_paper`（蓝色透光叠加） | **P1** |

---

## Shader 穿透物品-Mask 对应表

> 碎片0001为 MONITORED 模式——**不使用全局灰阶 Shader**（培训场景是公司建造的，不是 AI 碎片投影）。因此本碎片无 Shader 穿透物品。
>
> **注意**：如果后续决定为碎片0001叠加灰阶 Shader（模拟"公司培训场景也在万象碎片中"的叙事），则以下物品为建议的穿透候选（每个碎片 3-7 个穿透物品）：

| # | 候选物品 ID | 穿透区域 | 穿透色值参考 | 穿透饱和度 | 叙事依据 |
|---|-----------|---------|------------|-----------|---------|
| 1 | `0001_fx_light_wall_transparent` | 光墙透明外景——草地、树、天空的色彩 | `#90A870`（草地绿）/ `#D0D8E0`（天空蓝） | 40-50% | "外面是真实的世界——有颜色"——暗线C种子 |
| 2 | `0001_prop_dawn_seal` | 晨曦之印徽章边缘淡金光晕 | `#D4B880`（暖金） | 60-80% | 源印——在灰阶世界中天然应该是色彩残留最强的地方 |
| 3 | `0001_prop_chamber_crack` | 暗室地面裂缝透出的彩色光 | `#FFE0C0`（暖橙） | 15-20% | "灰阶世界底部有色彩渗入"——暗示 #0762「颜色的葬礼」 |
| 4 | `0001_prop_npc_badge` | 胸牌上的"冥府协议"小字——微弱的红色 | `#604040`（暗红） | 10-15% | 暗线B——"冥府"意象的颜色锚点（红色=危险/协议） |
| 5 | `0001_env_light_wall` | 蓝色光墙——在灰阶中保留蓝色 | `#6088D0`（中蓝） | 25-35% | 光墙是培训场景中唯一的"色彩"——在灰阶中保留此特征 |

---

## 附录A：物品-叙事关联索引

| 物品 ID | 关联叙事节拍 | 关联暗线 | 关联 NPC |
|---------|------------|---------|---------|
| `0001_prop_handwritten_note` | 钟楼暗室发现便笺 → "她不是工具" | 暗线A（"AI不是敌人"种子） | — |
| `0001_prop_npc_badge` | NPC胸牌"冥府协议" | 暗线B（"公司在说谎"种子） | 全部四个 NPC |
| `0001_prop_boundary_floor_inscription` | 光墙刻字"上一个你" | 暗线C（"你不止一个"种子） | 赵安保（关联） |
| `0001_prop_sundial_c_inscription` | 日晷底座"第3批测试" | 暗线B（"公司在测试你"证据） | — |
| `0001_prop_linguide_datapad` | 林指导数据板乱码 → 暗示记忆被清除 | 暗线A（公司控制NPC） | 林指导 |
| `0001_prop_chamber_crack` | 暗室裂缝透出彩色光 → "颜色存在于灰阶之下" | 跨暗线——预告 #0762 | — |
| `0001_prop_dawn_seal` | 晨曦之印显现 → 第一个源印 | 主线（核心玩法） | 林指导（通关祝贺） |
| `0001_fx_light_wall_transparent` | 光墙透明外景 → "外面是真实世界" | 暗线B+暗线C | 赵安保 |
| `0001_prop_lab_camera_sundial` | 日晷D上方摄像头 → "你在被观察" | 暗线B（监视） | — |
| `0001_prop_admin_screen` | 行政中心电子屏第47秒照片 → 王主管的家人 | 暗线B（公司的控制手段） | 王主管 |
| `0001_prop_newspaper` | 广场长椅报纸日期"2077年3月14日"→ 零时前一天 | 暗线B（零时的时间锚点） | — |
| `0001_prop_sundial_e_ruins` | 日晷E被砸碎 → "有人在隐藏什么" | 暗线C（"上一个你"可能砸的） | 赵安保 |

---

## 附录B：与 style_bible.md 的合规自查要点

| 自查项 | 状态 | 说明 |
|--------|------|------|
| 所有物品外轮廓保留轻微不规则剪刀切边（建筑除外——建筑不允许撕边） | ✅ | 道具类物品描述中已标注"纸艺裁切"或"手工裁切"。建筑类物品明确标注"外轮廓为直线/规则切割" |
| 不使用纯黑 `#000000` 和纯白 `#FFFFFF` | ✅ | 最暗色为 `#302820`（近黑），最亮色为 `#F0ECE0`（近白——灰阶碎片雕像特许范围） |
| 建筑保留了门/窗/屋檐/入口朝向/占地面积轮廓 | ✅ | 全部建筑物品包含"门的位置和宽度""窗""屋檐""入口朝向""占地面积"描述（参见 §四·4.1-B 的可读性底线） |
| 光源方向统一左上（阴影向右下） | ✅ | 所有物品阴影偏移遵循 L0-L4 层级表（规范 §五·5.3） |
| 每件物品分配了正确的材质族（从 6 种 Core 材质中选择） | ✅ | 全部物品的"材质"字段使用 `kraft`/`gray_card`/`book_page`/`fiber_paper`/`tracing_paper`/`foil` 其中之一或多者 |
| `book_page` 材质物品不包含可读文字 | ✅ | 所有文件和书籍类物品描述中标注"不可读文字""模糊书写痕迹""模糊字块" |
| 不出现光滑矢量边缘 | ✅ | 所有物品描述中注明"纸艺切割/手工裁切"，不使用矢量光滑边缘 |
| `foil` 材质克制使用（单物品 ≤10% 面积） | ✅ | 所有箔纸使用处标注了"<5%面积"或"≤10%面积" |

---

> *碎片 #0001「启程之镇」物品清单 — 文档结束*
> *配套文件：`0001_manifest.json`（待生成）、`0001_sp_mask_对应表.md`（已包含在本文 §10）*
